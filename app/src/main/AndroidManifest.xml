<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <package android:name="com.google.android.apps.maps" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gamstop">

        <activity
            android:name=".registration"
            android:exported="false" />

        <activity
            android:name=".reward_page"
            android:exported="false" />

        <activity
            android:name=".login_page"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".frontend.pre_resgistration.PreRegistrationActivity"
            android:exported="false" />

        <activity
            android:name=".frontend.smart_recommendations.SmartRecommendationsActivity"
            android:exported="false" />

        <activity
            android:name=".frontend.navigation.GpsActivity"
            android:exported="false" />

        <activity
            android:name=".frontend.calorie_stats.DailyCalStatsActivity"
            android:exported="false" />

    </application>

</manifest>